services:
  wapiti:
    build:
      context: "../../"
      dockerfile: "Dockerfile.integration"
    container_name: wapiti
    networks:
      - test-network
networks:
  test-network:
    external: true
